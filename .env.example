# Frontend API URL
# For local development, point to backend running on localhost
# In production (Vercel), this can be omitted to use the default relative /api path
VITE_API_URL=http://localhost:5000/api
